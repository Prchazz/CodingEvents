<h1>@ViewBag.title</h1>
<form method="post" action="/events/edit">
    <div class="form-group">
        <input type="hidden" value="@ViewBag.eventToEdit.Id" name="eventId" />
    </div>
    <div class="form-group">
        <label for="name">Name</label>
        <input name="name" value="@ViewBag.eventToEdit.Name" type="text" />
    </div>
    <div class="form-group">
        <label for="description">Description</label>
        <input name="description" value="@ViewBag.eventToEdit.Description" />
    </div>
    <div class="form-group">
        <label for="contactEmail">Description</label>
        <input name="contactEmail" value="@ViewBag.eventToEdit.ContactEmail" />
    </div>
    <input type="submit" value="Edit Event" />
</form>
